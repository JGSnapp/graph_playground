# Журнал экспериментов

Порядок работы — [IMPROVEMENT_LOOP.md](IMPROVEMENT_LOOP.md).
Типы записей: **P** промпт, **S** скилл, **T** тул, **G** гейт, **M** метрика,
**A** алгоритм, **B** баг, **I** инфраструктура.

---

## Состояние стенда

| Что | Значение |
|---|---|
| Пул задач | 8 задач, `bench/tasks.ts` |
| Модели базового прогона | `claude-sonnet-5`, `gpt-5.6-terra`, `deepseek-v4-flash` |
| Офлайн-набор | 14 сохранённых досок: 8 реальных из `data/boards.json` + 6 из прогона E01 |
| Метрика | `boardQuality` из `shared/src/quality.ts`, сравниваем по `cost` |

**Важно про офлайн-набор.** Это настоящие доски, собранные агентом в реальных
прогонах. Эксперименты на них не требуют API и полностью воспроизводимы:
берём готовую пару «узлы + связи» и меняем только тот этап, который проверяем.

---

## E00 — снятие базовой линии на реальных досках

- **Тип:** M (замер, без изменений в системе)
- **Что делали:** отрендерили все 9 сохранённых досок в PNG и посчитали качество.
- **Числа:**

| доска | модель | score | cost | узлы/связи | главные дефекты |
|---|---|---|---|---|---|
| brd_iebele45ar | deepseek-v4-flash | 18 | 114 | 8/8 | 12 пересечений, 2 слипания |
| brd_zkuvpvwpm3 | deepseek-v4-flash | 11 | 202 | 10/13 | 5 стрелок сквозь блоки, 15 пересечений |
| brd_fh2rorj78p | gpt-5.6-sol | 22 | 90 | 19/12 | 6 конфликтов подписей |
| brd_cc95er6lpx | deepseek-v4-flash | 7 | 350 | 11/12 | 8 сквозь блоки, 2 наложения, 16 подписей |
| brd_6iwhu2896m | deepseek-v4-flash | 9 | 250 | 23/10 | 29 пересечений, 13 слипаний |
| brd_tbcy37ybs3 | deepseek-v4-flash | 20 | 97 | 11/11 | 11 пересечений |
| brd_blbe2739z6 | deepseek-v4-flash | 5 | 521 | 11/12 | **64 пересечения, 25 слипаний** |
| brd_vl1t4dbj48 | deepseek-v4-flash | 19 | 104 | 12/11 | 18 пересечений |
| brd_6jw5yjbsa5 | deepseek-v4-flash | 63 | 15 | 8/0 | 1 наложение (набор карточек) |

- **Глазами:** у всех графовых досок один и тот же дефект — узлы стоят двумя
  колонками с широким пустым промежутком, и **все стрелки сливаются в одну
  вертикальную «шину»** посреди этого промежутка. Подписи при этом висят
  далеко от своих линий, потому что якорь подписи попадает на длинный общий отрезок.
  Текст в карточках 220×140 обрезается на полуслове.
- **Вывод:** это не единичные промахи модели, а один системный дефект. Дальше
  надо понять, чей он — расстановки или маршрутизации.

---

## E01 — базовый прогон пула на трёх моделях

- **Тип:** I
- **Результат:** прогон **не доведён**: на середине закончился баланс API-ключа
  (`insufficient_balance`, баланс ушёл в −37.87). Успели: 7 задач на
  `claude-sonnet-5`, 1 на `gpt-5.6-terra`.
- **Что успели увидеть:**
  - `cards-set` (набор карточек, без связей) — 100/100 у всех, кто дошёл;
  - `tree-org` — 23/100, `states-order` — 33/100, `next-to-occupied` — 51/100;
  - в первом же тестовом прогоне (`deepseek-v4-flash`, pipeline-cicd) из 31 итерации
    **14 ушло на отказы `arrow_create`**, а на 19-й итерации агент удалил все
    11 стрелок и начал заново.
- **Побочные наблюдения → отдельные записи:** B01 (400 на Claude 5), G01 (петля отказов).
- **Статус:** прогон нужно повторить после пополнения баланса.

---

## B01 — вся линейка Claude 5 и kimi-k3 отваливались с 400

- **Тип:** B
- **Симптом:** `claude-sonnet-5` возвращал `400 invalid_request` на каждый запрос.
- **Диагноз:** проверка по матрице моделей — DS Lab отвергает `temperature` для
  `claude-opus-5`, `claude-sonnet-5`, `claude-fable-5`, `kimi-k3`, `gpt-5.6-*`.
  В `allowsSamplingParams` были перечислены только GPT-5/o-series и два Opus 4.x.
- **Изменение:**
  - `server/src/modules/llm/provider.ts`: список расширен регуляркой на всю
    линейку Claude 5 и на `kimi-k3`;
  - там же в `chat` добавлен **один слепой повтор без sampling-параметров**
    при ответе 400 — чтобы следующая новая модель не стала неработающей молча.
- **Проверка:** матрица из 15 моделей — 200 на всех; тесты провайдера обновлены.
- **Вердикт:** принято. Список — быстрый путь, ретрай — страховка.

---

## A01 — виноват не роутер, а расстановка

- **Тип:** M (диагностика перед изменением)
- **Гипотеза 1 (не подтвердилась):** «шина» возникает из-за нехватки параллельных
  дорожек в широком пустом промежутке — сетка Ханана строится из рамок блоков,
  и все стрелки берут одну и ту же линию.
- **Что сделали:** вынесли `MAX_LANES_PER_GAP` в опцию `lanesPerGap`
  (`shared/src/routing.ts`), прогнали свип 4/8/12/16 на 14 досках.
- **Числа:** средний cost **114.4 при любом числе дорожек** — эффект ровно нулевой.
- **Гипотеза 2:** дело в самой расстановке. Прогнали на худшей доске
  (`brd_blbe2739z6`, 11 узлов, 12 стрелок) свип по всем параметрам роутера:

| вариант | cost | пересечений |
|---|---|---|
| как есть | 521 | 64 |
| роутер по умолчанию | 288 | 50 |
| lanesPerGap=16 | 288 | 50 |
| overlapPenalty=8 | 266 | 41 |
| crossPenalty=200 | 303 | 51 |
| turnPenalty=40 | 298 | 50 |
| turn40+overlap8+lanes16 | 255 | 36 |

- **Вывод:** 12 стрелок дают ~50 пересечений при **любых** настройках прокладки.
  Пересечения задаются порядком узлов в слое, а не формой линий. Это ровно то,
  что предсказывал [QUALITY_MODEL §8.2](QUALITY_MODEL.md): роутер оптимизирует
  `boardQuality`, а `boardQuality` ничего не знает о расстановке узлов.
- **Вердикт:** `lanesPerGap` оставлен как опция (не мешает, полезен для будущих
  экспериментов), значения по умолчанию не изменились. Дальше — A02.

---

## A02 — послойная раскладка графа (`shared/src/layout.ts`)

- **Тип:** A
- **Гипотеза:** если разложить узлы по слоям вдоль потока и подобрать порядок
  внутри слоя медианной эвристикой, пересечения уйдут почти полностью, и роутеру
  останется только аккуратно провести линии.
- **Изменение:** новый модуль `shared/src/layout.ts`:
  - разрыв циклов обходом в глубину (обратные рёбра переворачиваются, не удаляются);
  - слои методом длиннейшего пути;
  - порядок внутри слоя — медианная эвристика со сцеплением групп
    (узлы одной группы двигаются как блок);
  - выравнивание координат по медиане соседей с перепаковкой слоя;
  - сжатие пустых полос (`compactBands`) — равномерный сдвиг, прямые связи не ломаются;
  - `arrangeGraph` — раскладка + прокладка + оценка: перебирает 2 направления × 2 шага
    и оставляет вариант с лучшей `boardQuality`.
- **Числа на 14 досках:**

| вариант | средний cost | лучше базы |
|---|---|---|
| как есть (ручная расстановка агента) | 135 | — |
| только перепрокладка роутером | 114 | 6/14 |
| **arrangeGraph (раскладка + прокладка)** | **26** | **14/14** |

  Пересечения рёбер после раскладки: 0 на 13 досках из 14 (на одной — 3).
- **Глазами:** худшая доска (`brd_blbe2739z6`, была 5/100) превратилась в читаемую
  схему с потоком слева направо и нулём пересечений. «Шина» исчезла.
  Осталось: сжатие пустых полос срабатывает не всегда (порог `spacing.node * 2`),
  текст в старых карточках 220×140 по-прежнему обрезан — это дефект размеров,
  а не раскладки.
- **Вердикт:** принято.

---

## T01 — инструмент `board_arrange_graph`

- **Тип:** T
- **Изменение:** `server/src/modules/agent/tools/arrange.tools.ts`.
  Параметры: `nodeIds`, `direction` (`auto` по умолчанию), `groups`, `lockIds`,
  `nodeSpacing` / `layerSpacing` / `groupSpacing`, `originX/Y`, `dryRun`.
  Артефакты только двигаются — ничего не создаётся и не удаляется.
  Отказывается работать на доске без стрелок (это набор карточек, не граф).
- **Отключение:** удалить `...arrangeTools` из `ALL_TOOLS` в `tools/index.ts` — одна строка.
- **Тесты:** `server/test/arrange.test.ts` — 10 тестов: слои, разрыв цикла,
  снижение пересечений, отсутствие наложений, `lockIds`, выбор направления,
  применение к доске, `dryRun`, отказ без стрелок.
- **Вердикт:** принято.

---

## G01 — гейты стрелок чинят вместо отказа

- **Тип:** G
- **Гипотеза:** отказ `arrow_create` стоит целого раунда с моделью (~25 c и токены),
  а исправление в обоих случаях механическое: свободный порт на той же стороне
  или перпендикулярный «усик». Гейт, который умеет починить, должен чинить.
- **Замер до изменения:** симуляция «наивная сетка + все стрелки с auto-сторонами»
  на 14 досках → **32 отказа** гейтов (2.3 на схему).
- **Изменение:** `arrow.tools.ts`: `shallowPortRefuse` + `mixedPortRefuse` заменены
  на `repairPorts`:
  1. конфликт входа/выхода → свободный `offset` на той же стороне;
  2. если места нет → ближайшая сторона, где место есть;
  3. плоский подход (<30°) → `ensureHeadOnBends` добавляет перпендикулярный изгиб;
  4. отказ остаётся **только** если ни на одной стороне блока нет места —
     это уже решение о раскладке, которое должен принять агент.
  Всё, что тул поправил сам, возвращается в поле `adjustments`.
- **Числа после изменения:** 124 вызова `arrow_create` через реальный реестр тулов →
  **0 отказов**, 37 автопочинок, средний cost после `board_arrange_graph` — 26.
- **Глазами:** проверено, что автопочинка не создаёт новых дефектов —
  `arrowSharedPort` после неё равен нулю.
- **Вердикт:** принято. Гейт на наложение артефактов оставлен отказом: там выбор
  места — содержательное решение, а не механика.

---

## P01 / S01 — промпт и скилл под порядок «сначала связи, потом раскладка»

- **Тип:** P + S
- **Гипотеза:** новый инструмент бесполезен, пока промпт учит подбирать координаты руками.
- **Изменение:**
  - `settings/defaults.ts`: раздел «Как работать» переписан под порядок
    **узлы → все стрелки → один `board_arrange_graph` → точечные правки**.
    Явно сказано не мучиться с координатами и не задавать стороны/порты до раскладки.
    В «Законах раскладки» добавлено: «пересечения решаются порядком узлов,
    а не изгибами»; убрано устаревшее про периметр и про отказ смешанного порта.
  - `skills/seed.ts`: `graph-layout` переписан в 6 шагов вокруг `board_arrange_graph`,
    с числом из замера A02 как обоснованием. В `artifact-set` добавлен явный запрет
    вызывать раскладку графа для набора карточек.
- **Замечание:** промпт и скиллы хранятся в `data/`, поэтому правка кода **не доходит
  до работающей установки сама**. В UI: «Вернуть промпт по умолчанию» в настройках
  промпта и «Восстановить встроенные» в панели скиллов. Для стенда — `bench/sync-prompt.ts`.
- **Проверка:** 196 тестов (server + web) зелёные.
- **Вердикт:** принято, но **эффект на живых моделях не измерен** — нужен прогон
  E02 после пополнения баланса.

---

## A03 — узлы без связей и пропорции композиции

- **Тип:** A + M
- **Что заметили:** после A02 средний `aspect` ухудшился с 1.96 до 2.96, а на
  `brd_6iwhu2896m` дошёл до **15.25** — композиция превратилась в ленту.
  Причина: узлы без единой связи (заголовки секций, отдельные карточки) получали
  слой 0 по методу длиннейшего пути и выстраивались в один гигантский ряд.
  На той доске таких узлов было 13 из 23.
- **Изменение 1:** узлы без рёбер исключены из слоёв и выносятся отдельным
  компактным блоком за последним слоем; форма блока — не тоньше квадрата
  и не глубже самого графа.
- **Изменение 2:** выбор варианта в `arrangeGraph` учитывает вытянутость:
  к `cost` добавляется небольшой штраф за stretch выше 2.5. Штраф намеренно мал —
  он разводит близкие варианты, но никогда не перевешивает реальный дефект.
- **Числа (14 досок, до → после раскладки):**

| показатель | до | после A02 | после A03 |
|---|---|---|---|
| средний cost | 135.4 | 25.7 | 26.1 |
| aspect | 1.96 | 2.96 | **1.44** |
| separation | 1.46 | 1.80 | 1.67 |
| худший aspect | — | 15.25 | 2.23 |

- **Вердикт:** принято. Стоимость — +0.4 к среднему cost, выигрыш — композиция
  перестала вырождаться в ленту.

---

## M01 — переопределение наблюдательной метрики соседства

- **Тип:** M
- **Проблема:** метрика `neighbourFidelity` («ближайший сосед узла — тот, с кем он
  связан») после A02 **падала**, хотя картинка становилась заметно лучше.
  Она штрафует именно то, что делает послойная раскладка: ближайший сосед узла
  в слое — это его сосед по слою, с которым он законно не связан.
- **Изменение:** заменена на `separation` — отношение среднего расстояния до
  несвязанных узлов к среднему расстоянию до связанных. Выше 1 означает
  «связанное стоит ближе чужого», что и есть закон читаемого расстояния.
- **Числа:** на 14 досках `separation` растёт с 1.46 до 1.67 — то есть раскладка
  улучшает и смысловое расстояние, а не только геометрию.
- **Заодно добавлена** наблюдательная метрика `textOverflow`: сколько узлов
  содержат текст, не помещающийся в заданный размер. На реальных досках это
  в среднем 3 узла из ~12, в худшем случае 22 из 23, и движок этого не видит вовсе.
- **Вердикт:** принято. `textOverflow` пока только наблюдательная — в движок
  переедет после того, как появится, чем на неё отвечать (H1).

---

## E02 — полный пул на deepseek-v4-flash после всех правок

- **Тип:** прогон
- **База сравнения:** E00 (те же 8 реальных досок, та же модель, старая система).
- **Стоимость:** 2 893 206 входных + 40 934 выходных токенов за 121 запрос, 8 досок.
  Резерв шлюза ≈ 45 единиц ≈ $0.7; **95% входных токенов пришли из кэша**.

| задача | score | ok | узл/свз | итер | отказы | ×× | aspect |
|---|---|---|---|---|---|---|---|
| cards-set | **100** | ✅ | 9/0 | 4 | 0 | 0 | 1.29 |
| next-to-occupied | **96** | ✅ | 13/7 | 9 | 0 | 0 | 0.45 |
| wiki-anime | 74 | — | 8/10 | 17 | 0 | 2 | 0.49 |
| tree-org | 61 | — | 12/11 | 8 | 0 | 2 | 1.52 |
| states-order | 60 | — | 8/10 | **50** | **7** | 3 | 0.08 |
| extend-existing | 58 | — | 8/9 | 15 | 0 | 2 | 0.64 |
| pipeline-cicd | 50 | — | 10/12 | 20 | 0 | 6 | 0.28 |
| dense-arch | **19** | — | 14/18 | 15 | 0 | **24** | 3.02 |

**Итог: средний score 65 против ~14 у E00.**

- **Ворота 1 взяты:** ноль наложений артефактов и ноль стрелок, режущих блоки,
  во всех восьми прогонах. В E00 их было 2 и 13 соответственно.
- Отказов гейтов: 7 на весь пул против ~14 за один прогон в исходном замере,
  и все семь — это отказ `artifact_move` по наложению, то есть тот гейт,
  который намеренно оставлен решением агента.
- Скилл работает: агент читает `graph-layout`, создаёт узлы, затем все стрелки,
  затем зовёт `board_arrange_graph`. Порядок соблюдён во всех графовых задачах.
- `cards-set` — 100/100 и **ни одного** вызова раскладки: разделение «граф против
  набора карточек» держится.

### Что осталось плохо

1. **`dense-arch`, 24 пересечения стрелок** — 96 из 109 штрафа. Разбор в A04.
2. **`states-order`, 50 итераций и 1.6M токенов** — 56% токенов всего эксперимента
   ушли в один прогон. После раскладки агент не стал править точечно, а свалился
   в цикл «подвинуть все узлы руками → удалить все 10 стрелок → создать их заново
   → проложить», и повторил его пять раз. Чекпоинт спас результат (восстановлена
   итерация 9, качество 60), но цена прогона выросла на порядок.
3. **Пропорции:** aspect 0.08–0.64 у пяти задач из восьми — композиции вытянуты
   в вертикальную ленту.
4. `ok=true` только у 2 из 8, и во всех остальных единственная причина —
   `arrow_arrow`. Подтверждает H3: требование нуля пересечений недостижимо.

---

## A04 — фиктивные узлы для длинных рёбер: гипотеза отвергнута

- **Тип:** A
- **Наблюдение:** на `dense-arch` раскладка даёт **0 пересечений рёбер**, а на доске
  их 24. Значит порядок узлов верный, а расходятся именно нарисованные линии.
  Причина видна на картинке: рёбра, идущие через несколько слоёв
  (`Клиент → CDN`, `Хранилище видео → CDN`, три линии в `Postgres`), тянутся
  напрямик через всю схему. У них нет выделенной дорожки, и эвристика минимизации
  пересечений их не видит, потому что сравнивает только соседние слои.
- **Гипотеза:** классический шаг Сугиямы — разбить длинное ребро фиктивными узлами
  по промежуточным слоям, включить их в минимизацию пересечений и вести линию
  по зарезервированным дорожкам.
- **Изменение:** фиктивные узлы в `layoutGraph` (дорожка шириной 28px в каждом
  промежуточном слое), `LayoutResult.waypoints`, привязка длинных рёбер к дорожкам
  в `arrangeGraph` с прокладкой роутером только коротких рёбер.
- **Числа (A/B на 21 сохранённом случае):**

| вариант | средний cost | лучше |
|---|---|---|
| без дорожек | **26** | 20/21 |
| с дорожками | 56 | 1/21 |

  При этом число пересечений рёбер в раскладке **идентично** в обоих вариантах
  (0/0, 3/3, 5/5). То есть фиктивные узлы не изменили порядок, а весь вред нанесла
  моя привязка полилиний: стороны прибиваются к `right→left` независимо от того,
  что ребро развёрнутое, а участок от порта до первой дорожки не выстраивается
  ортогонально.
- **Вердикт:** привязка **отключена** (`useLanes`, по умолчанию false).
  Фиктивные узлы в слоях оставлены: они нейтральны по метрике (26 = 26) и делают
  `crossings` честным числом, учитывающим длинные рёбра. Правильная реализация
  требует аккуратного построения ортогональной полилинии через дорожки —
  это отдельная гипотеза, а не доделка этой.
- **Урок:** «алгоритм из учебника» не помогает сам по себе, если стыковка с
  реальным рендерером сделана грубо. Мерить надо связку, а не идею.

---

## A05 — тематические полосы через все слои: гипотеза отвергнута

- **Тип:** A
- **Гипотеза:** сцепка групп сейчас работает только внутри слоя, поэтому тема может
  оказаться сверху в одном слое и снизу в другом. Единый порядок групп через всю
  композицию («плавательные дорожки») должен сделать подсистемы читаемыми пятнами.
- **Изменение:** `groupBands` в `LayoutOptions` — общий порядок тем, вычисленный
  по медианному положению их участников, как первичный ключ сортировки в слое.
- **Замер:** темы заданы явно по смыслу задач (`bench/groups-exp.ts`), метрика
  компактности — средний разброс участников темы вокруг своего центра,
  делённый на разброс всей композиции.

| | средний cost | компактность тем |
|---|---|---|
| без полос | **48** | 0.58 |
| с полосами | 63 | 0.56 |

- **Вердикт:** отклонено, флаг `groupBands` по умолчанию выключен. Полосы стоят
  +31% штрафа, а компактность улучшают на 3% — сцепка внутри слоя уже даёт
  практически весь эффект тематической группировки.
- **Побочно:** прогоны E02 обрезали аргументы вызовов до 240 символов, из-за чего
  реальные группы модели невозможно было переиграть. Исправлено: `bench/run.ts`
  сохраняет аргументы целиком.

---

## M02 — калибровка стоимости по фактическим логам

- **Тип:** M
- До E02 константы в `bench/cost.ts` были оценками. Теперь они измерены
  (`bench/calibrate.ts` читает `runs.json`):

| | было (оценка) | стало (замер) |
|---|---|---|
| промпт на вызов | 9 000 | **23 100** |
| итераций на доску | 14 | **17** |
| максимальный ответ | — | 2 189 токенов |
| доля кэша во входных токенах | — | **95%** |

- **Следствия:**
  - оценка резерва для дешёвой модели выросла с 2 до 6 единиц за доску — прежняя
    была занижена втрое, потому что бралась длина промпта первой итерации;
  - `maxTokens: 4096` подтверждён как разумный: самый длинный ответ за весь пул —
    2 189 токенов;
  - 95% кэша означают, что **фактический счёт много меньше резерва**, и что
    сокращение схем инструментов — не первый рычаг цены, как я считал раньше:
    повторяющийся префикс промпта и так почти не оплачивается.

---

## E03 — порог ok по плотности + запрет пересборки

- **Тип:** прогон (проверяет G02 «бюджет пересечений» и S02 «запрет пересборки»)
- **База сравнения:** E02, тот же пул, та же модель.

| задача | score | итераций | пересечений |
|---|---|---|---|
| cards-set | 100 → 100 | 4 → 8 | 0 → 0 |
| next-to-occupied | 96 → **97** | 9 → 8 | 0 → 0 |
| tree-org | 61 → **74** | 8 → **40** | 2 → 2 |
| pipeline-cicd | 50 → **76** | 20 → **9** | 6 → 2 |
| wiki-anime | 74 → 71 | 17 → 13 | 2 → 2 |
| states-order | 60 → **68** | **50 → 10** | 3 → 3 |
| extend-existing | 58 → **39** | 15 → **50** | 2 → 7 |
| dense-arch | 19 → **27** | 15 → 11 | 24 → 15 |

| показатель | E02 | E03 |
|---|---|---|
| средний score | 65 | **69** |
| ok | 2/8 | **5/8** |
| отказов всего | 7 | **3** |
| пересечений всего | 39 | **31** |
| средн. итераций | 17 | 19 |
| входных токенов | 3.19M | 3.45M |

### Что сработало

- **Бюджет пересечений сделал своё дело:** `ok` вырос с 2/8 до 5/8, и это не
  послабление метрики — `cost` не менялся, а средний score вырос независимо (65 → 69)
  и общее число пересечений упало (39 → 31).
- **Названный антипаттерн исчез там, где его назвали:** `states-order` с 50 итераций
  и 1.6M токенов ушёл на 10 итераций и 153k — в 10 раз дешевле при score 60 → 68.
  `pipeline-cicd` — 20 → 9 итераций и 50 → 76 баллов.

### Что не сработало

**Цикл не исчез, а переехал.** Я запретил конкретный антипаттерн («удалить связи
и создать заново»), и агент нашёл другой: `extend-existing` сделал 19 вызовов
`artifact_move` вперемешку с `board_route_arrows` и `board_clean_arrows`,
упёрся в лимит 50 итераций и потратил 1.55M токенов; `tree-org` — 40 итераций
вместо 8. Суммарный расход токенов по пулу **вырос** (3.19M → 3.45M).

Вывод: запрет в тексте скилла лечит ровно ту формулировку, которую назвали.
Нужен механический стоп, а не уговоры: у цикла прогона уже есть оценка качества
на каждой итерации, и после нескольких подряд без улучшения агенту надо это
сказать явно, а не надеяться, что он прочитает вердикт.

### Глазами

- `pipeline-cicd`: в E02 Push ветвился сразу в Линтер и Юнит-тесты (структурная
  ошибка) и линии завязывались узлом посередине; в E03 — правильная цепочка
  сверху вниз, ветки fail аккуратно сходятся в Slack, пересечений почти нет.
- `next-to-occupied`: шесть существующих карточек не тронуты, новая цепочка
  инцидента построена рядом, ноль пересечений. Ровно то поведение, которого ждём.
- `extend-existing`: картинка читается неплохо, но две линии «кэш» к Redis идут
  через всю ширину композиции — метрика штрафует справедливо.
- `dense-arch`: по-прежнему худший. Слои видно с трудом, длинные рёбра тянутся
  через всё поле. Это та же проблема, что в A04: длинным рёбрам негде идти.

### Следующее

1. Механический стоп по застою — вместо текстового запрета (новая гипотеза H12).
2. H9 — корректная привязка длинных рёбер, главный оставшийся алгоритмический долг.

---

## Что не проверено и ждёт бюджета API

| Что | Как проверять |
|---|---|
| Пользуется ли модель `board_arrange_graph` без напоминаний | E02: полный пул × 3 модели, смотреть `toolHistogram` |
| Упало ли число итераций | E02 vs E01: колонка «итер» в отчёте |
| Не сломался ли сценарий «дополнить готовый граф» | задача `extend-existing`: `lockIds` должен удержать старые узлы |
| Не начал ли агент звать раскладку для набора карточек | задача `cards-set`: вызовов `board_arrange_graph` должно быть 0 |
| Слабая модель против сильной | разрыв по score между `deepseek-v4-flash` и `claude-sonnet-5` |

Команда для E02 (после пополнения):

```bash
node_modules/.bin/tsx bench/sync-prompt.ts bench/data
TECA_DATA_DIR=./bench/data PORT=5199 node_modules/.bin/tsx server/src/index.ts
node_modules/.bin/tsx bench/run.ts --exp=E02 --task=all \
  --model=claude-sonnet-5,gpt-5.6-terra,deepseek-v4-flash --concurrency=3
node_modules/.bin/tsx bench/report.ts --exp=E01,E02
```

---

## Очередь гипотез

| # | Тип | Гипотеза | Основание |
|---|---|---|---|
| H1 | M | Метрика «текст не влезает в карточку» | На каждой реальной доске текст обрезан, метрика этого не видит |
| H2 | ~ | Пропорции учтены при выборе варианта раскладки (A03), но в `boardQuality` их по-прежнему нет | Лента 4000×300 и квадрат 1200×900 получают одинаковую оценку движка |
| H3 | M | Смягчить `ok`: бюджет пересечений от плотности графа | **Подтверждено в E02**: у 6 из 8 задач единственная причина `ok=false` — `arrow_arrow` |
| H4 | A | Сжатие пустых полос работает не всегда | Видно на `brd_blbe2739z6` после A02 |
| H5 | T | Тул «свободное место рядом» вместо сырого списка артефактов | Нет понятия занятой зоны, задача `next-to-occupied` — 51/100 |
| H6 | A | Ортогональная раскладка для деревьев отдельно от слоёв | Дерево в слоях выглядит шире, чем нужно |
| H8 | S+T | Не дать агенту пересобирать композицию: после раскладки править точечно, а не удалять и создавать связи заново | `states-order` в E02 съел 56% токенов эксперимента на пять кругов «удалить всё и создать заново» |
| H11 | ~ | Тематические полосы отвергнуты (A05); компактность тем даёт сцепка внутри слоя | — |
| H12 | T | Механический стоп: после N итераций без улучшения качества сказать агенту явно, что пора заканчивать | E03: запрет одного антипаттерна в тексте просто переселил цикл в другой |
| H9 | A | Правильная привязка длинных рёбер к дорожкам (ортогональная полилиния, учёт развёрнутых рёбер) | A04 отвергнут из-за грубой стыковки, сама идея не проверена |
| H10 | M | Пропорции композиции в самой метрике, а не только в выборе варианта | aspect 0.08–0.64 у пяти задач E02 |
| H7 | ✅ | Сделано в M01 | — |
