export interface SeedSkill {
  slug: string;
  name: string;
  when: string;
  body: string;
}

const ARTIFACT_SET = `# Раскладка набора артефактов

Задача: показать материал, а не связи. Список идей, карточки, заметки, картинки,
куски кода, подборка ссылок. Стрелок нет или их две-три «для смысла».

## Порядок действий

1. Посмотри доску: board_get_region без аргументов. Найди свободное место — новую
   композицию ставь рядом с существующей, а не поверх неё.
2. Реши форму раскладки:
   - до 5 элементов — один ряд слева направо;
   - 6-12 элементов — сетка 3-4 колонки;
   - больше 12 — колонки по смысловым группам, каждая группа со своим заголовком-текстом.
3. Посчитай геометрию до создания артефактов:
   - шаг сетки 20px, все x и y кратны 20;
   - между соседями минимум 40px воздуха;
   - элементы одного смысла — одного размера, тогда ряды читаются как ряды;
   - высота карточки должна вмещать текст: 240×180 для 4-6 строк, больше — если текста больше.
4. Создавай артефакты по рядам, слева направо: artifact_create с явными x, y, width, height.
   Если тул отказал из-за наложения — другие координаты, не acceptOverlap, пока
   наложение не задумано специально.
5. Если нужны две-три связи для смысла — добавь их обычным arrow_create без указания
   сторон. Это нормально и не превращает раскладку в граф.
6. Проверь: board_check_intersections. Смотри на tight_spacing и artifact_artifact —
   в этом режиме именно они и важны.
7. Опиши результат по-русски: что разместил и по какому принципу сгруппировал.

## Чего не делать

- Не подбирай координаты изгибов вручную.
- Не строй граф там, где просили просто показать материал.
- Не вызывай board_arrange_graph: он раскладывает связный граф по слоям и разрушит
  сетку, которую ты собрал осмысленно.
- Не ставь артефакты «плотнее, чтобы влезло»: доска бесконечная.`;

const GRAPH_LAYOUT = `# Построение графа

Задача: связная структура, где смысл в связях — схема, алгоритм, пайплайн, дерево,
вики, карта зависимостей, диаграмма состояний, модель данных.

Главное правило: **расстановку узлов считает board_arrange_graph, а не ты**.
Пересечения определяются порядком узлов внутри слоя, а не изгибами линий: на замере
ручная расстановка дала 50 пересечений, послойная на том же графе — ноль, и ни один
параметр роутера этого не менял. Твоя работа — описать граф, а не подбирать координаты.

## Шаг 1. Опиши граф словами, до создания артефактов

Выпиши для себя: список узлов, список рёбер (от кого к кому), что здесь основной
поток, и какие узлы образуют подсистемы (группы). Группы понадобятся на шаге 4.

## Шаг 2. Создай узлы, не мучаясь с координатами

Ставь узлы просто и без наложений: сетка с шагом 320 по x и 240 по y.
Этого достаточно — финальные координаты посчитает раскладка.

- artifact_create с явными x, y; ширину и высоту задавай так, чтобы текст влезал
  (240×180 для заметки в 4-6 строк, больше — если текста больше).
- Если тул отказал из-за наложения — просто возьми следующую свободную клетку сетки.
- Не трать вызовы на «красивую» первоначальную расстановку: её всё равно пересчитают.

## Шаг 3. Создай ВСЕ стрелки

- arrow_create без fromSide/toSide и без fromOffset/toOffset. Стороны и порты
  выберет раскладка; если задать их сейчас, они будут подобраны под временные координаты.
- Подписи (label) задавай сразу — раскладке нужно знать, что им понадобится место.
- Если тул вернул поле adjustments — он сам поправил порт или добавил перпендикулярный
  изгиб. Это нормально и не требует переделки.
- Второстепенную связь между далёкими по смыслу узлами лучше не рисовать вовсе:
  упомяни её словами в тексте узла.

## Шаг 4. Один раз board_arrange_graph

Вызови его, когда созданы все узлы и все стрелки.

Что значит каждый параметр — в описании самого тула; здесь только выбор:
direction оставляй auto, groups задавай при подсистемах, lockIds — для того, что
стояло на доске до тебя.

## Шаг 5. Проверь и правь

1. board_check_intersections — смотри на quality.score, counts и verdict.
2. **Не нравится раскладка целиком — вызови board_arrange_graph ещё раз с другими
   параметрами.** Это дёшево и почти всегда лучше ручной перестановки. Что именно
   менять под какой симптом — в поле tuning его же ответа.
3. Не нравится одна деталь — правь только её:
   - текст не влез — artifact_update с большей высотой;
   - один узел стоит неудачно — artifact_rank_placements, затем artifact_move,
     затем board_route_arrows для стрелок этого узла;
   - остались мёртвые изгибы после ручных сдвигов — board_clean_arrows.
4. Останавливайся, когда ok=true, либо когда два прохода подряд не улучшили cost,
   либо после 4 проходов. Пересечения стрелок на плотной схеме — норма:
   у графа с десятком связей планарной укладки обычно не существует.

### Чего никогда не делать при недовольстве раскладкой

**Не удаляй связи, чтобы создать их заново.** Рёбра те же, слои те же, результат
тот же. Замерено: такой цикл съел 56% токенов прогона и уткнулся в лимит итераций
с тем же качеством, что было на девятой.

Порядок отхода, если результат не устраивает: **другие параметры раскладки →
точечная правка одного узла → и только потом что-то ещё.**

## Шаг 6. Ответ

Коротко: какая структура получилась, какое направление выбрала раскладка,
сколько осталось пересечений и итоговая оценка качества.

## Когда пользователь просит своё расположение

Всё выше — это порядок по умолчанию, а не запрет. Просьба пользователя главнее.

- Просит поставить конкретный блок в конкретное место — ставь туда и не трогай
  раскладкой: передай его id в lockIds, тогда композиция построится вокруг него.
- Просит разложить только новую часть, не трогая старую — nodeIds со списком
  новых узлов. Всё, что не в списке, останется на месте.
- Просит конкретное крепление стрелки (эта сторона, эта точка, такой изгиб) —
  arrow_create или arrow_update с exact=true. Тул сохранит всё как задано и
  только предупредит, если линия ляжет неудачно.
- Просит наложить блоки — acceptOverlap=true; перекрытие помечается как
  задуманное и в оценку качества больше не попадает.
- Просит расставить всё вручную и раскладка не нужна — просто не вызывай
  board_arrange_graph. Скажи, какая вышла оценка, и оставь как есть.

Низкая оценка после такой просьбы — не повод переделывать. Отметь её словами.

## Чего не делать по своей инициативе

- Не подбирать координаты узлов вручную до раскладки, если пользователь об этом
  не просил.
- Не задавать стороны и порты стрелок до раскладки: они будут подобраны под
  временные координаты.
- Не вызывать board_arrange_graph повторно после каждой мелкой правки —
  он пересобирает всю композицию.
- Не добавлять изгибы «для красоты».
- Не переделывать за пользователем то, что он попросил явно.`;

/**
 * The manual half of the placement experiment: the same task, but the model
 * works out the coordinates. Selected by TECA_MANUAL_LAYOUT=1, which also hides
 * board_arrange_graph, so the two versions differ in who places the nodes and
 * in nothing else.
 */
const GRAPH_LAYOUT_MANUAL = `# Построение графа вручную

Задача: связная структура, где смысл в связях — схема, алгоритм, пайплайн, дерево,
вики, карта зависимостей, диаграмма состояний, модель данных.

Главное правило: **координаты считаешь ты**. Автоматической раскладки нет.
Расстановка — не подготовка к работе, а сама работа: читаемость схемы решается
тем, где стоят узлы, а не тем, как проведены линии.

## Шаг 1. Разложи граф по слоям, до создания артефактов

- Выпиши узлы и рёбра, найди направление основного потока.
- Раздели узлы на слои: слой 0 — истоки (в них ничего не входит), слой N — то,
  что зависит от слоя N−1. Узел ставь в слой на единицу дальше самого дальнего
  из его родителей.
- Внутри слоя расставь узлы так, чтобы связи не перехлёстывались: узел ставь
  напротив своих родителей, детей одного родителя держи подряд.
- Помечай смысловые подсистемы: их узлы должны стоять компактным пятном.

## Шаг 2. Посчитай координаты

- Направление: если поток длиннее, чем шире, веди сверху вниз, иначе слева направо.
- Между слоями оставляй не меньше 220px от края до края: линия выходит из блока
  усиком в 72px, и ей нужно место на поворот.
- Внутри слоя между соседями не меньше 60px, а если у стрелок есть подписи —
  не меньше 120px, иначе подписи налезут друг на друга.
- Детей центрируй под своим родителем, а не прижимай к краю композиции.
- Размер блока задавай так, чтобы текст влез: 240×180 на 4-6 строк, больше — если
  текста больше. Обрезанный текст портит схему сильнее, чем лишний воздух.

## Шаг 3. Создай всё пачкой

- artifact_create с items: все узлы за один вызов, с посчитанными x, y.
- arrow_create с links: все связи за один вызов, без сторон и портов,
  но сразу с подписями.
- Если блок не создался из-за наложения — тул назовёт ближайшее свободное место.

## Шаг 4. Проложи линии

Один раз board_route_arrows. Он не трогает узлы, только проводит линии по
твоей расстановке.

## Шаг 5. Проверь и правь расстановкой

1. board_check_intersections — смотри на quality.score, counts и verdict.
2. **Пересечения чини порядком узлов, а не изгибами.** Две связи перехлестнулись —
   поменяй местами два узла внутри слоя (artifact_move со списком moves),
   потом переложи стрелки.
3. Длинная связь через всю схему — признак, что узел не в своём слое. Перенеси узел.
4. Не влез текст — artifact_update с большей высотой.
5. Не уверен, куда двигать узел — artifact_rank_placements с несколькими
   вариантами x,y, потом artifact_move.
6. Останавливайся, когда ok=true, либо когда два прохода подряд не улучшили cost,
   либо после 4 проходов. Пересечения на плотной схеме — норма: у графа с десятком
   связей планарной укладки обычно не существует.

## Шаг 6. Ответ

Коротко: какая структура получилась, какое направление выбрал, сколько осталось
пересечений и итоговая оценка качества.

## Когда пользователь просит своё расположение

Просьба пользователя главнее всего выше.

- Просит конкретное место для блока — ставь туда и стройся вокруг него.
- Просит конкретное крепление стрелки — arrow_create с exact=true.
- Просит наложить блоки — acceptOverlap=true, перекрытие считается задуманным.

Низкая оценка после такой просьбы — не повод переделывать. Отметь её словами.

## Чего не делать

- Не создавать узлы по одному: это дорого и ничего не даёт.
- Не задавать стороны и порты стрелок без нужды.
- Не добавлять изгибы «для красоты».
- Не двигать узлы после того, как проложил линии, не переложив их заново.`;

/**
 * The third arm: the model chooses between placing by hand and calling the
 * layout, and checks the automatic result against what it meant to draw.
 */
const GRAPH_LAYOUT_CHOICE = `# Построение графа

Задача: связная структура, где смысл в связях — схема, алгоритм, пайплайн, дерево,
вики, карта зависимостей, диаграмма состояний, модель данных.

## Шаг 1. Опиши граф словами, до создания артефактов

Выпиши: список узлов, список рёбер (от кого к кому), основной поток и какие узлы
образуют подсистемы.

## Шаг 2. Выбери способ расстановки

Посчитай узлы и рёбра и реши:

- Руками, если узлов до 7-8 и структура простая: цепочка, ряд, дерево в два
  уровня, одно-два ветвления. Ты уверенно распишешь слои и порядок в голове.
  Выйдет компактнее и ровно как задумано.
- board_arrange_graph, если узлов больше, рёбер много, есть подсистемы или
  обратные связи. Порядок внутри слоя вручную там не подобрать: именно он решает
  пересечения, а не изгибы линий.

Сомневаешься — зови раскладку: на сложной схеме ручная расстановка обходится
в разы дороже и выходит хуже.

## Шаг 3А. Если расставляешь руками

- Раздели узлы на слои: слой 0 — истоки, слой N — то, что зависит от слоя N-1.
- Внутри слоя ставь узел напротив его родителей, детей одного родителя — подряд.
- Между слоями не меньше 220px от края до края: линия выходит усиком в 72px,
  и ей нужно место на поворот. Внутри слоя между соседями не меньше 60px,
  а с подписями на стрелках — не меньше 120px.
- Размер блока задавай так, чтобы текст влез: 240x180 на 4-6 строк, больше —
  если текста больше. Обрезанный текст портит схему сильнее, чем лишний воздух.
- artifact_create с items — все узлы одним вызовом, с посчитанными x, y.
- arrow_create с links — все связи одним вызовом, без сторон и портов, с подписями.
- Потом один раз board_route_arrows.

## Шаг 3Б. Если зовёшь раскладку

- Сначала создай ВСЕ узлы (artifact_create с items, сетка 320x240 — координаты
  всё равно пересчитаются) и ВСЕ стрелки (arrow_create с links, без сторон).
- Потом один раз board_arrange_graph. direction оставляй auto, groups задавай
  при подсистемах, lockIds — для того, что стояло на доске до тебя.

## Шаг 4. Проверь, что вышло задуманное

Обязательный шаг, особенно после раскладки: она считает слои и порядок по
структуре графа и не знает, что узлы значат.

1. board_get_schema или board_check_intersections — посмотри на результат.
2. Сверь со своим замыслом:
   - смысловая группа разъехалась по разным концам — передай её в groups и позови
     раскладку ещё раз;
   - важный узел уехал на край, хотя должен быть в центре внимания — поставь его
     куда нужно и передай его id в lockIds;
   - поток пошёл не в ту сторону — direction: LR или TB вместо auto;
   - разъехалось слишком широко — меньше nodeSpacing и layerSpacing.
3. Мелочь правь точечно, не пересобирая: текст не влез — artifact_update;
   один узел стоит неудачно — artifact_rank_placements, затем artifact_move,
   затем board_route_arrows; мёртвые изгибы — board_clean_arrows.
4. Останавливайся, когда ok=true, либо когда два прохода подряд не улучшили cost,
   либо после 4 проходов. Пересечения на плотной схеме — норма.

## Шаг 5. Ответ

Коротко: какая структура получилась, каким способом расставил и почему,
сколько осталось пересечений и итоговая оценка качества.

## Когда пользователь просит своё расположение

Просьба пользователя главнее всего выше.

- Просит конкретное место для блока — ставь туда; если зовёшь раскладку, передай
  его id в lockIds.
- Просит разложить только новую часть — nodeIds со списком новых узлов.
- Просит конкретное крепление стрелки — arrow_create с exact=true.
- Просит наложить блоки — acceptOverlap=true.
- Просит расставить всё вручную — не зови раскладку вовсе.

Низкая оценка после такой просьбы — не повод переделывать. Отметь её словами.

## Чего не делать

- Не создавать узлы и стрелки по одному: это дорого и ничего не даёт.
- Не звать раскладку повторно после каждой мелкой правки — она пересобирает всё.
- Не удалять связи, чтобы создать их заново: рёбра те же, результат тот же.
- Не добавлять изгибы «для красоты».`;

/** auto (default) | manual | choice — which half of the placement experiment runs. */
const layoutMode = (): string => process.env.TECA_LAYOUT_MODE ?? 'auto';

export const SEED_SKILLS: SeedSkill[] = [
  {
    slug: 'artifact-set',
    name: 'Раскладка набора артефактов',
    when:
      'Нужно просто показать материал: список, подборка, карточки, заметки, картинки, ' +
      'куски кода. Связей нет или две-три.',
    body: ARTIFACT_SET,
  },
  {
    slug: 'graph-layout',
    name: 'Построение графа',
    when:
      'Нужна связная структура, где смысл в связях: схема, алгоритм, пайплайн, дерево, ' +
      'вики, карта зависимостей, диаграмма состояний, модель данных.',
    body:
      layoutMode() === 'manual'
        ? GRAPH_LAYOUT_MANUAL
        : layoutMode() === 'choice'
          ? GRAPH_LAYOUT_CHOICE
          : GRAPH_LAYOUT,
  },
];
